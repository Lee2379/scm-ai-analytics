# A/B Test Simulation and SCM KPI Impact Evaluation

## Purpose

This document explains the A/B test simulation added to the SCM analytics portfolio. The goal is to show how AI-driven replenishment and store-transfer recommendations can be evaluated against a baseline inventory policy using business KPIs, not only dashboard visuals.

This is a portfolio-grade simulation based on reproducible demo data. It is not presented as a live production A/B test.

## Japanese Summary

本ドキュメントは、AIによる補充推奨・店舗間在庫移動推奨が、従来の在庫運用方針と比較してSCM KPIをどの程度改善できるかを検証するA/Bテストシミュレーションの設計を説明します。

実運用の本番A/Bテストではなく、公開用デモデータに基づくポートフォリオ向けの効果検証フレームワークです。

## Experiment Design

| Item | Definition |
| --- | --- |
| Experimental unit | `store_id × sku_id` |
| Evaluation horizon | 28-day demand forecast |
| Control policy | Baseline ROP replenishment policy |
| Treatment policy | AI replenishment recommendation plus store-transfer recommendation |
| Primary KPI | Total SCM cost proxy |
| Guardrail KPIs | Stockout rate, overstock rate, service level, lost sales proxy, holding cost, transfer cost |

## Policy Logic

### Control: Baseline ROP Policy

The baseline policy orders up to the reorder point only:

```text
control_order_qty = max(ROP - stock_on_hand, 0)
```

This reflects a simple inventory policy that reacts to the reorder point but does not optimize for the 28-day demand forecast or store-transfer opportunities.

### Treatment: AI Recommendation Policy

The treatment policy uses the recommendation table generated by the SCM engine:

```text
treatment_order_qty = recommended_order_qty
available_units = stock_on_hand + treatment_order_qty + inbound_transfer_qty - outbound_transfer_qty
```

This represents an AI-assisted policy that uses forecast demand, safety stock, and transfer opportunities.

## KPI Definitions

| KPI | Formula |
| --- | --- |
| Fulfilled units | `min(available_units, forecast_28d)` |
| Lost sales units | `max(forecast_28d - fulfilled_units, 0)` |
| Ending inventory | `max(available_units - forecast_28d, 0)` |
| Service level | `fulfilled_units / forecast_28d` |
| Stockout flag | `lost_sales_units > 0` |
| Overstock flag | `ending_days_of_supply > 42` |
| Lost sales proxy | `lost_sales_units × unit_price` |
| Holding cost | `ending_inventory × unit_price × 1.5%` |
| Order handling cost | `order_qty × JPY 40` |
| Transfer cost | `(inbound_transfer_qty + outbound_transfer_qty) × JPY 120` |
| Total SCM cost proxy | `lost_sales proxy + holding cost + order handling cost + transfer cost` |

## Result Summary

| KPI | Control | Treatment | Impact |
| --- | ---: | ---: | ---: |
| Stockout rate | 71.7% | 1.7% | -70.0 pp |
| Service level | 64.4% | 99.8% | +35.4 pp |
| Lost sales proxy | JPY 52,092,554 | JPY 153,230 | -JPY 51,939,324 |
| Total SCM cost proxy | JPY 52,510,587 | JPY 1,616,646 | -96.9% |

## Business Interpretation

The simulation suggests that the largest improvement opportunity is reducing forecast-period stockouts by moving from a reactive ROP-only policy to a forecast-aware replenishment policy. The treatment policy increases order and transfer costs, but the reduction in lost-sales proxy dominates the cost tradeoff.

For a real company setting, this framework would be used before rollout to answer:

- Which stores or product categories should be prioritized first?
- Which recommendations reduce stockout risk without creating overstock?
- How much transfer cost is acceptable relative to lost-sales reduction?
- Which KPI should be used as the primary success metric?

## Portfolio Relevance

This addition strengthens the project from a dashboard demo into a business-impact evaluation case study. It shows the ability to connect SCM analytics, AI recommendations, KPI design, and policy evaluation in a way that is relevant for Japanese retail, manufacturing, logistics, trading-company, and DX roles.
